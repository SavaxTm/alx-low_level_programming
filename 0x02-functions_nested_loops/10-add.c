#include <stdio.h>
#include "main.h"
/**
 * add - adds two integers
 * @int : parameter for two integers to be added
 * Return : a + b
 */
int add(int, int);
int add (int a, int b)
{
	return (a + b);
}
