#include <stdio.h>
#include "_putchar.h"
/**
* main - Entry point
*
* Return - Always 0 (Success)
*/
int main(void)
{
	void print_alphabet(void);
	
		for (x = 'a'; x <= 'z'; ++x);

		printf("%c", x);

	return (0);
}
