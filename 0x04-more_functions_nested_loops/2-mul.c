#include "main.h"
#include <stdio.h>
/**
 * mul - multiplies two integers
 *
 * @a : defines integer to be multiplied
 * @b : defines the second integer
 *
 * Return: a * b (Success)
 */
int mul(int a, int b)
{
	return (a * b);
}
