#include <stdio.h>
#include "main.h"
/**
 * _isdigit - Entry Point
 *
 * @Param c - Parameter to determine integers
 *
 * Return: 1 (Success)
 * Onerror return 0
 */
int _isdigit(int c)
{
	for (c = 48; c <= 57; c++)
	{
		return (1);
	}
	return (0);
}
